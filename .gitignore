# Internal working notes: job-search intent, positioning, and things said
# in confidence. This repo is public, so these must never be committed.
PRODUCT.md

# Tooling scratch
.impeccable/
.hallmark/
.playwright-mcp/

# Full-size video masters; the page serves the compressed -720 versions
assets/videos/*-landscape.mp4
